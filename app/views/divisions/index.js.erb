window.history.pushState("", "", "<%= request.fullpath  %>");
$("#division").html("<%= escape_javascript(render(:partial => 'index', :locals => {:divisions => @divisions})) %>");
LoadPage();
<% if params[:format] == "js" %>
window.history.pushState("", "", "<%= request.fullpath.gsub('.js','')  %>");
<% end %>