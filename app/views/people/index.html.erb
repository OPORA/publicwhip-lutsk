<% content_for :title, "Депутати — Як Голосує Твій Депутат?" %>

<div id="first-block" class="container">

    <div class="row justify-content-lg-center">
      <h1 class="col-lg-8 text-q text-uppercase">Депутати <%= Settings.name_rada %></h1>

      <div class="col-lg-8">
        <%= form_tag(search_mp_path, method: "get", class: "form-inline mt-2 mt-md-0", id: "search-main" ) do  %>
                              <span class="fa-stack">
                                <i class="fa fa-search fa-stack-1x fa-inverse" aria-hidden="true"></i>
                              </span>
            <%= text_field_tag(:mp, params[:mp] , class: "form-control mr-sm-2" , placeholder: "напр., " + placeholder , list: "members-datalist", autocomplete: "off" ) %>
            <i class="fa fa-chevron-down arrow-icon" aria-hidden="true"></i>
            <datalist id="members-datalist">
              <% @mp.each do |m| %>
                  <option value="<%= m.full_name %>"></option>
              <% end %>
            </datalist>
        <% end %>
       <script>  
        $(document).ready(function($) {
          $('#mp').change(function() {
            $('#search-main').submit();
          });
      });
      </script>
      </div>
    </div>
</div>

  <div id="profile" class="container">
    <%= render(:partial => 'index', :locals => {:mps => @mps}) %>
  </div>

<script>
    //added slider filter
    window.onload = function() {
        LoadPagePeople();
    };
</script>